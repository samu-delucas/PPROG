#ifndef TYPES_H
#define	TYPES_H

#include <stdio.h>
#include <stdlib.h>

typedef struct _NPC NPC;
typedef struct _Global Global;

typedef enum{
    ERROR = -1,
    OK = 0
} Status;

typedef enum {
    FALSE=0,
    TRUE=1
} Bool;



#endif